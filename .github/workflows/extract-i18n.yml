name: Extract i18n Strings

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - '.github/workflows/extract-i18n.yml'
      - 'scripts/extract_i18n_strings.py'
      - 'application/xmodem/files/usr/share/xmodem/**'
      - 'luci/luci-app-xmodem-next/htdocs/luci-static/resources/**'
  pull_request:
    paths:
      - '.github/workflows/extract-i18n.yml'
      - 'scripts/extract_i18n_strings.py'
      - 'application/xmodem/files/usr/share/xmodem/**'
      - 'luci/luci-app-xmodem-next/htdocs/luci-static/resources/**'

permissions:
  contents: write

jobs:
  extract-i18n:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          
      - name: Extract i18n strings
        run: |
          python3 scripts/extract_i18n_strings.py \
            --sh-path application/xmodem/files/usr/share/xmodem \
            --js-path luci/luci-app-xmodem-next/htdocs/luci-static/resources \
            --format po \
            --output luci/luci-app-xmodem-next/po/template/xmodem-next.po
            
      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code luci/luci-app-xmodem-next/po/template/xmodem-next.po || echo "changed=true" >> $GITHUB_OUTPUT
          
      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true' && github.event_name == 'push'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add luci/luci-app-xmodem-next/po/template
          git commit -m "chore: update i18n template strings [skip ci]"
          git push
